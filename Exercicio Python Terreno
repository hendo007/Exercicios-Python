"""
Programa para ler dimensões de um terreno e o preço do metro da tela, imprimindo o valor para cercar o terreno.
"""

comp = input("Digite o comprimento (em metros) do terreno: ")  # Recebe do valor do comprimento do terreno (em metros)

larg = input("Digite a largura (em metros) do terreno: ")  # Recebe do valor da largura do terreno (em metros)

p = input("Digite o valor do metro de tela: ")  # Recebe o valor do metro da tela

print(f"O custo de tela para cercar esse terreno sera de:R${((int(comp)*2) + (int(larg)*2)) * int(p)},00")
# Retorna o preço para cercar esse terreno
