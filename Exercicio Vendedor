"""
Programa que recebe um valor total e, a partir dele, retorna:

- O valor com 10% de desconto;
- O valor de cada parcela, parcelado em 3x sem juros;
- A comissão do vendedor, 5% sobre o valor com desconto, caso a venda seja a vista;
- A comissão do vendedor, 5% sobre o valor total, caso a venda seja parcelada;
"""

valor = input("Digite o valor da compra: ") 

print(f"Valor com 10% de desconto: {int(valor) - (int(valor) * 0.1)}") 

print(f"Valor das parcelas (em 3x sem juros): {int(valor)/3}")

print(f"Comissão do vendedor (se a venda for a vista): {(int(valor) - (int(valor) * 0.1)) * 0.05}")

print(f"Comissão do vendedor (se a venda for parcelada): {int(valor) * 0.05}")
